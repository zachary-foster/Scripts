statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
my_test <- 2
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
my_test
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
View(my_test)
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
View(my_test)
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
my_test
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
my_test
quantile(my_test, .8, na.rm=TRUE, type=3)
quantile(my_test, .9, na.rm=TRUE, type=3)
max(my_test)
seq(0,  quantile(my_test, .8, na.rm=TRUE, type=3), by = .001)
seq(0,  quantile(my_test, .9, na.rm=TRUE, type=3), by = .001)
table(my_test)
max(distance_matrix)
min(distance_matrix)
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
my_before = 1
my_after = 1
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
my_before
my_after
melt(my_after, measure.vars=4:6, id.vars = 1,  na.rm=TRUE)
data <- melt(my_after, measure.vars=4:6, id.vars = 1,  na.rm=TRUE)
my_plot <- ggplot(data[data$variable != "cumulative_error", ], aes(x=threshold, y=value)) +
geom_area(aes(fill = variable), alpha = .3, , position='identity') +
geom_line(data=data[data$variable == "cumulative_error", ], position='identity') +
labs(title=title) +
scale_x_continuous(limits = c(min_x_display, max_x_display)) +
scale_y_continuous(limits = c(min_y_display, max_y_display)) +
theme(title=element_text(size=30),
axis.line=element_blank(),
axis.title.x=element_blank(),
axis.title.y=element_blank(),
legend.position="none",
panel.background=element_blank(),
panel.border=element_blank(),
panel.grid.major=element_blank(),
panel.grid.minor=element_blank(),
plot.background=element_blank())
source("functions.R")
my_plot <- ggplot(data[data$variable != "cumulative_error", ], aes(x=threshold, y=value)) +
geom_area(aes(fill = variable), alpha = .3, , position='identity') +
geom_line(data=data[data$variable == "cumulative_error", ], position='identity') +
labs(title=title) +
scale_x_continuous(limits = c(min_x_display, max_x_display)) +
scale_y_continuous(limits = c(min_y_display, max_y_display)) +
theme(title=element_text(size=30),
axis.line=element_blank(),
axis.title.x=element_blank(),
axis.title.y=element_blank(),
legend.position="none",
panel.background=element_blank(),
panel.border=element_blank(),
panel.grid.major=element_blank(),
panel.grid.minor=element_blank(),
plot.background=element_blank())
my_plot
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
my_plot
my_test
print(my_test)
dev.off
dev.off()
dev.off()
dev.off()
dev.off()
dev.off()
dev.off()
dev.off()
dev.off
dev.off
dev.off()
dev.off()
dev.off()
dev.off()
dev.off()
dev.off()
print(my_test)
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
my_test
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
my_test
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
traceback()
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001,
max_sequences_to_compare = 100))
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001,
max_sequences_to_compare = 500))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001,
max_sequences_to_compare = 500))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001,
max_sequences_to_compare = 500))
which(1:10 > 30)[1]
which(1:40 > 30)[1]
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001,
max_sequences_to_compare = 500))
sd(1)
Na /NA
NA /NA
1 + Na
1 + NA
NA * 3
source("functions.R")
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001,
max_sequences_to_compare = 500))
source("functions.R")
#Calculate statistics
statistics <- lapply(clustering_levels, function(x)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= x,
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001,
max_sequences_to_compare = 500))
View(distance_matrix)
calculate_barcode_statistics(distance_matrix, taxonomy_levels,
level_to_analyze= 'p',
saved_output_path = output_directory,
save_statistics = TRUE,
save_raw_data = TRUE,
save_plots = TRUE,
distance_bin_width = 0.001,
threshold_resolution = 0.001,
max_sequences_to_compare = 500)
